<?xml version="1.0" encoding="utf-8"?>
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:uap10="http://schemas.microsoft.com/appx/manifest/uap/windows10/10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  xmlns:desktop6="http://schemas.microsoft.com/appx/manifest/desktop/windows10/6"
  xmlns:virtualization="http://schemas.microsoft.com/appx/manifest/virtualization/windows10"
  IgnorableNamespaces="rescap desktop6 virtualization">

  <Identity
    Name="abusayed.dev.BanglaSaver"
    Version="1.0.0.0"
    Publisher="CN=8EA73F0A-0DC1-449F-A896-493A34649C49"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>BanglaSaver</DisplayName>
    <PublisherDisplayName>abusayed.dev</PublisherDisplayName>
    <Description>বাংলা স্ক্রিনসেভার - A beautiful Bangla digital clock screensaver for Windows featuring Bengali calendar, Bangla numerals, and seasonal display.</Description>
    <Logo>Assets\StoreLogo.png</Logo>

    <!--
      Registry write virtualization must be disabled so that the screensaver
      registration (HKCU\Control Panel\Desktop\SCRNSAVE.EXE) is visible to
      the Windows screensaver service. Without this, the registry write would
      be virtualized to a per-app private hive and Windows would never
      activate the screensaver.
    -->
    <desktop6:RegistryWriteVirtualization>disabled</desktop6:RegistryWriteVirtualization>

    <!--
      On Windows 11+, use fine-grained exclusion instead of disabling all
      HKCU virtualization. This is more secure as only the required key
      is unvirtualized. On older Windows, the desktop6 property above is used.
    -->
    <virtualization:RegistryWriteVirtualization>
      <virtualization:ExcludedKeys>
        <virtualization:ExcludedKey>HKEY_CURRENT_USER\Control Panel\Desktop</virtualization:ExcludedKey>
      </virtualization:ExcludedKeys>
    </virtualization:RegistryWriteVirtualization>
  </Properties>

  <Resources>
    <Resource Language="en-us" />
    <Resource Language="bn-BD" />
  </Resources>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.19041.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
    <rescap:Capability Name="unvirtualizedResources" />
  </Capabilities>

  <Applications>
    <Application
      Id="BanglaSaver"
      Executable="BanglaSaver.exe"
      uap10:RuntimeBehavior="packagedClassicApp"
      uap10:TrustLevel="mediumIL">

      <uap:VisualElements
        DisplayName="বাংলাসেভার"
        Description="Bangla digital clock screensaver with Bengali calendar"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png"
        BackgroundColor="#006A4E" />

    </Application>
  </Applications>

</Package>
